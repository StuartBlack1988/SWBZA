
import { toast as sonnerToast } from "sonner";

// Re-export the toast function for consistent use across the app
export const toast = sonnerToast;

// Export the toast function as the default export
export default toast;
